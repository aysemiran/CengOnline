<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cengonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CommentActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessageActivity" />
        <activity android:name=".ForgotPassActivity" />
        <activity android:name=".StudentCourseActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".TeacherCourseActivity" />
        <activity android:name=".StreamActivity" />
        <activity android:name=".EditDeleteTeacherActivity" />
        <activity android:name=".EditDeleteCourseActivity" />
        <activity android:name=".AddCourseActivity"/>
        <activity android:name=".AddStudentsCourseActivity"/>
    </application>

</manifest>